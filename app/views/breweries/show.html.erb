<p id="notice"><%= notice %></p>


<h2>
  <%= @brewery.name %>
</h2>

<p>
  <em>Established at <%= @brewery.year %></em>
</p>

<p>
  <strong>Number of beers: <%= @brewery.beers.count %></strong>
</p>
<p>
  <% @brewery.beers.each do |beer| %>
    <%= link_to beer.name, beer %>
  <% end %>
</p>

<p>
  <% if @brewery.ratings.empty? %>
  <% else %>

    This brewery has <%= pluralize(@brewery.ratings.count,'rating') %>. Average rating for this brewery is <%= @brewery.average_rating %>
  <% end %>
</p>

<%= link_to 'List of beers', beers_path  %>
<%= link_to 'Edit', edit_brewery_path(@brewery) %> |
<%= link_to 'Back', breweries_path %>

<h1>New membership</h1>

<%= form_for (@membership) do |m| %>
  <%= m.select :beer_club_id, options_from_collection_for_select(@beer_clubs, :id, :name) %>
  <%= m.submit %>
<% end %>
<%= link_to 'Back', memberships_path %>
